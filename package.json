{
  "name": "be-the-mayor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "check": "npx tsc --noEmit",
    "dev-nolog": "vite",
    "build": "tsc && vite build",
    "build-nolog": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
